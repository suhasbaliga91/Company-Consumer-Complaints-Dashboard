/** Amber "SAMPLE RUN" badge shown on panels where extraction is partial. */
export function SampleTag() {
  return (
    <span className="inline-flex items-center rounded-sm border border-amber-500/40 bg-amber-500/10 px-1.5 py-0.5 font-mono text-[9px] font-medium tracking-widest text-amber-400 uppercase">
      Sample Run
    </span>
  );
}
